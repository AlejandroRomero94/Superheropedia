package com.alejandro.superheropedia.domain.usecase

import com.alejandro.superheropedia.domain.Repository
import javax.inject.Inject

class SuperheroUseCase @Inject constructor(private val repository: Repository){
   // suspend operator fun invoke(superheroId:String)=repository.getSuperheroDetail(superheroId)

    /*

    NIÑO, ATIENDE. TIENES QUE HACER UN USE CASE PARA CADA LISTA, HEROES Y VILLANOS

    LOS BOTONES VAN A SER IGUALES QUE EL SEARCHVIEW, SOLO QUE A LA VARIABLE TEXT, EN VEZ
    DE METERLE UN STRING A TRAVÉS DE UNA LAMBDA O NTEXT CHANGED, DIRECTAMENTE
    SE LA HARDCODEAS PARA QUE HAGA EL MATCH. ES MUY SIMPLE!!!!!


     */




}